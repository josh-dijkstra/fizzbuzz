package com.saggezza.techtrack.techexcellence.fizzbuzzproject;

import org.junit.jupiter.api.Test;

import java.io.IOException;

import static org.mockito.Mockito.mock;

public class FizzBuzzCMDTest {

    private FizzBuzzCMD cmd;

    @Test
    public void readInput() throws IOException {
        FizzBuzzCMD cmd = mock(FizzBuzzCMD.class);
        //when(cmd.readIO()).thenReturn();
        //assertEquals(getBoundIntegerFromUser(cmd), 3);
    }

}
